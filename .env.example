# Backend server URL (defaults to https://bar3-server.onrender.com)
# The server handles all Discord OAuth (start, callback, session cookie).
# Discord redirect URI must be set on the SERVER, not the client.
# Example server redirect URI: https://bar3-server.onrender.com/auth/discord/callback
VUE_APP_SERVER_URL=https://bar3-server.onrender.com
